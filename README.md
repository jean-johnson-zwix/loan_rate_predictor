# loan_rate_predictor

AWS-managed MLOps project predicting mortgage `rate_spread` (APR − APOR) on Arizona HMDA data (2021–2024).

## Setup

```bash
make data   # download AZ HMDA CSVs into data/raw/
```

## Docs

- `docs/FEATURE_SPEC.md` — architecture, stack decisions, non-negotiables
- `docs/PROGRESS.md` — build stage tracker
